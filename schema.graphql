type Query {
  jobs: [Job!]
  job(id: ID!): Job
}

type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  id: ID!
  title: String!
  """
  The __date__ when the job was published, in ISO-8601. E.g. '2023-02-01'.
  """
  date: String!
  description: String
  company: Company!
}
